#include <stdio.h>
int main ()
{
    int bball[4][5];
    int i,j,sum=0;
    for(i=0;i<4;i++)
        for(j=0;j<5;j++)
    {
        scanf("%d",&bball[i][j]);
        sum=sum+bball[i][j];
    }
    printf("Sum of points: %d",sum);
    return 0;

}
